# Minimal dependencies: custom logic only (no ML libraries).
# Use with: USE_LIGHT_MODE=1 python wsgi.py
# Install: pip install -r requirements-light.txt
#
# Not included: numpy, pandas, scikit-learn, torch, opencv, librosa, etc.
# Predictions use webapp.api.predict_light (custom threshold logic).
# Upload/combined video endpoints are disabled in light mode.

# Web
flask>=2.3.0
flask-cors>=4.0.0
werkzeug>=2.3.0
waitress>=3.0.0

# Config (optional; app works without config.yaml in light mode)
pyyaml>=6.0

# Auth and database
pymongo>=4.6.0
PyJWT>=2.8.0
bcrypt>=4.1.0
certifi>=2023.0.0
python-dotenv>=1.0.0
